.header
  .header-title
    Book Performance
  .register
    .book_register_group
      - if current_user
        %span.book_register
          = current_user.name
      - else
        = link_to "会員登録",new_user_registration_path,class: "book_register"
    .user_register_group      
      - if current_user
        = link_to "ログアウト", destroy_user_session_path,class: "user_register"
      - else
        = link_to  "ログイン",new_user_session_path,class: "user_register"